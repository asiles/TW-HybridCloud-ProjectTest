
---
- hosts: all
  vars:
    ansible_connection: network_cli
    ansible_network_os: ios

  tasks:
  - name: IOS | Show clock
    ios_command:
      provider: "{{ provider }}"
      commands:
        - show clock
    register: clock

  - debug: msg="{{ clock.stdout }}"